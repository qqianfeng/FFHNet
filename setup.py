#!/usr/bin/env python

from distutils.core import setup

setup(name='FFHNet',
      version='1.0',
      description='Hithand FFHNet',
      author='',
      author_email='',
      url='',
      packages=['FFHNet', 'FFHNet.utils', 'FFHNet.data', 'FFHNet.config', 'FFHNet.models'])
